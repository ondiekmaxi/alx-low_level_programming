#include <stdio.h>

/**
 * main - write a progrm that prints its name, followed by a new line
 * If you rename the programme it will print the new name, without having to
 * compile it once more
 * the path before the name of the program should not be removed
 *
 * @srg c: the argument count
 * @srgv: argument vector
 *
 * Return: 0
 */
int main(int argc, char *argv[v])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
